import React, { Component } from 'react'

class Failed extends Component {
  render(){
    return(
      <section className="text-center">
        <h2 className="font-weight-bold my-4">
          Sorry, we couldn't not process your payment
        </h2>
      </section>
    );
  }
}

export default Failed
